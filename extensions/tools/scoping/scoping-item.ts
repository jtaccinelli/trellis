/**
 * Tool: scoping-item
 *
 * Start a recursive, antagonistic, domain-scoped planning session for a work item.
 * Creates a session, root coordinator, and drives the delegation loop until stable.
 */

// pi.registerTool("scoping-item", { execute: ... })
