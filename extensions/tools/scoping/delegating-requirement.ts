/**
 * Tool: delegating-requirement
 *
 * Internal coordinator tool that enqueues a scope requirement onto a domain's
 * shared work queue. The queue manager serializes execution and spawns fresh
 * domain agents.
 */

// pi.registerTool("delegating-requirement", { execute: ... })
