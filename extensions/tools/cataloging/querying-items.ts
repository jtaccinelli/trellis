/**
 * Tool: querying-items
 *
 * List items in a domain, optionally filtered by status or contract shape.
 * Used by domain agents during reuse checks.
 */

// pi.registerTool("querying-items", { execute: ... })
