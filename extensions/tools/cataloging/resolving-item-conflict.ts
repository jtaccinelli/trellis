/**
 * Tool: resolving-item-conflict
 *
 * Mark an ambiguous catalogue decision as escalated or resolved.
 * Distinct from resolving-conflict, which operates on scope requirements.
 */

// pi.registerTool("resolving-item-conflict", { execute: ... })
