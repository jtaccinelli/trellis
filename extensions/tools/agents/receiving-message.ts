/**
 * Tool: receiving-message
 *
 * Poll the caller's own inbox. Long-lived agents use this to discover
 * steering notes and queue-manager completion notifications.
 */

// pi.registerTool("receiving-message", { execute: ... })
