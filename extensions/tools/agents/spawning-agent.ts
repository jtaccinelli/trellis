/**
 * Tool: spawning-agent
 *
 * Spawn a background subagent from inside another agent. Returns the new agent
 * id immediately so the caller can continue.
 */

// pi.registerTool("spawning-agent", { execute: ... })
