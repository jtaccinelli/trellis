/**
 * Tool: sending-message
 *
 * Send a one-to-one message to a running agent. Used by the user to steer
 * coordinators/background agents, and by the queue manager to notify a
 * coordinator that a work item completed.
 */

// pi.registerTool("sending-message", { execute: ... })
