/**
 * Slash command: /cataloging-project
 *
 * Interactive entry point that catalogues existing project items across all
 * configured domains. Useful for bootstrapping a new Trellis session.
 */

// pi.registerCommand("cataloging-project", ...)
