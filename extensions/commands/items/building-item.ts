/**
 * Slash command: /building-item
 *
 * Interactive entry point that starts building a ratified item.
 * May spawn a background agent via spawning-agent or enqueue build work.
 */

// pi.registerCommand("building-item", ...)
