/**
 * Slash command: /scoping-item
 *
 * Interactive entry point that starts a recursive domain-scoped scoping session.
 * Delegates to the scoping-item tool and opens the session inspector.
 */

// pi.registerCommand("scoping-item", ...)
