/**
 * Slash command: /reviewing-item
 *
 * Interactive entry point that starts a review pass over a built item.
 * Delegates to review agents and surfaces the result for sign-off.
 */

// pi.registerCommand("reviewing-item", ...)
